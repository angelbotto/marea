# Phase 3 — Notificaciones

**Goal:** Avisar con notificación nativa cuando el modo Auto prende o apaga un stack.

**Success:** permiso `UserNotifications` solicitado · notificación por acción con lo liberado
("Apagué `plane` (idle 40 min) → liberé 1.2 GB") · toggle en Preferencias · rate-limited.

Detalle completo en `../../ROADMAP.md` (Phase 3). Correr `/gsd-plan-phase 3`.
